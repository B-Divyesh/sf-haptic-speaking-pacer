# Haptic Speaking Pacer

Live: https://haptic-speaking-pacer.sociobot.in — built by the Param Factory (`ios-ipa`).

See `.factory/brief.json` for the researched problem this solves and `.factory/design.md` for the visual system.

## Develop

```
npm install
npm run dev
npm test
npm run build   # -> dist/
```
